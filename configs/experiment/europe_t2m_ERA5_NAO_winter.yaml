# @package _global_
defaults:
  - /paths/default

tags: ["noa_1940_2024_NAO", "winter"]

seed: 42

trainer:
  precision: 16-mixed

task_name: t2m_ERA5

hydra:
  run:
    dir: ${paths.log_dir}/${task_name}/NAO_winter
  sweep:
    dir: ${paths.log_dir}/${task_name}/NAO_winter
    subdir: ${hydra.job.num}

data:
  indexes_paths: 
    - ${paths.data_dir}/monthly_z500_NAO_DJF_noa_19400101-20241201.nc
  anomalies_path: ${paths.data_dir}/monthly_t2m_anom_europe_19400101-20241231_regrid.nc
  months: [12,1,2]
  num_indexes:
    - 1
  train_last_date: '2010-12-31'
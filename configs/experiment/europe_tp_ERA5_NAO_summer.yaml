# @package _global_
defaults:
  - /paths/default

tags: ["noa_1940_2024_NAO", "summer"]

seed: 42

trainer:
  precision: 16-mixed

task_name: tp_ERA5

hydra:
  run:
    dir: ${paths.log_dir}/${task_name}/NAO_summer
  sweep:
    dir: ${paths.log_dir}/${task_name}/NAO_summer
    subdir: ${hydra.job.num}

data:
  indexes_paths: 
    - ${paths.data_dir}/monthly_z500_NAO_JJA_noa_19400101-20241201.nc
  anomalies_path: ${paths.data_dir}/monthly_tp_anom_europe_19400101-20241231_merged.nc
  months: [6,7,8]
  num_indexes:
    - 1
  train_last_date: '2010-12-31'
# @package _global_
defaults:
  - /paths/default

tags: ["noa_1940_2024_7wr"]

train_last_year: '2005'
val_last_year: '2010'

seed: 42

trainer:
  precision: 16-mixed

task_name: tp_ERA5

hydra:
  run:
    dir: ${paths.log_dir}/${train_last_year}/${task_name}/7wr
  sweep:
    dir: ${paths.log_dir}/${train_last_year}/${task_name}/7wr
    subdir: ${hydra.job.num}

data:
  indexes_paths: 
    - ${paths.data_dir}/monthly_z500_7wr_noa_19400101-20241201.nc
  anomalies_path: ${paths.data_dir}/monthly_tp_anom_europe_19400101-20241231_regrid.nc
  months: []
  num_indexes:
    - 7
  train_last_date: '${train_last_year}-12-31'
  val_last_date: '${val_last_year}-12-31'
